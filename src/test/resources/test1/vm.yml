dev-server:
    proxyhost: localhost
    proxyport: 3000
    environments:
        - velocity
environments:
    velocity:
        templates: velocity
        files:
            folder:
                - "src/test/resources/test1"
